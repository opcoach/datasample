<project name="Copy model to src directory" default="copyModel" basedir=".">
	<description>
        This ant copy automatically the dataSample.ecore stored in model directory into the src/model directory
        It's necessary to be sure that model will be exported in classpath when this plugin will be used.'
    </description>

	<target name="copyModel">
		<!-- Create the project directory structure -->
		<echo message="copying model into src/model" />
		<mkdir dir="src/model"/>
		<copy todir="src/model" file="model/dataSample.ecore"/>
			
		<eclipse.refreshLocal  resource="com.opcoach.generator.ecore.dsgenmodel" depth="infinite" />
	</target>

	
</project>